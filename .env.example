# Only needed when the client is hosted separately from the game server
# (e.g. client on Netlify, server on Render). Leave unset for same-origin setups.
VITE_SERVER_URL=https://designsystemet-golf.onrender.com
